[Unit]
Description=ANet VPN Server
After=network.target
Wants=network.target
Before=network-online.target

[Service]
Type=simple
DynamicUser=yes
NoNewPrivileges=yes

StateDirectory=anet

# CAP_BPF нужен только если [shaper].enabled = true в server.toml.
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_BIND_SERVICE CAP_BPF
CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_BIND_SERVICE CAP_BPF

Environment=RUST_LOG=info

WorkingDirectory=/opt/anet
ExecStart=/opt/anet/anet-server -c /opt/anet/server.toml

Restart=on-failure
RestartSec=5s
TimeoutStopSec=30s
TimeoutStartSec=30s

ProtectClock=yes
ProtectKernelModules=yes
ProtectKernelTunables=yes
ProtectKernelLogs=yes
ProtectHome=yes
ProtectSystem=strict
ProtectProc=invisible

[Install]
WantedBy=multi-user.target
